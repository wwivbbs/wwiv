==============================================================================
                             WWIV 5.0 TODO List
                 Copyright 2002-2005 WWIV Software Services
==============================================================================

Move bbs logic out of WLocalIO and into WSession (probably)

Move WComm and WLocalIO references into WSession instead of WBbsApp

Rename WBbsApp to WApplication

Make WUser function names start with upper case like rest of code.

Convert ini code into a WIniFile class:
  ini_init => WIniFile::WIniFile 
  ini_done => WIniFile::~WIniFile
  ini_get => WIniFile::GetString(key), GetString(key, numberIndex), 
             GetString(key, stringIndex)
  ini_flags => WIniFile::GetFlags()

