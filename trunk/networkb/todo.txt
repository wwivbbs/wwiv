TODO for networkb
=================

pre-alpha
=========
# * Create File based TransferFile subclass for real use.
* create daemon process to be the listener side
* create unit tests
#* create server side of extension (OPT wwivnet) that if the server supports
#  it then use native wwivnet addressing (format: M_NUL WWIV @address.netname)
* figure out how to setup passwords for connections
#* figure out how to remove a node connection from ppp project
* create config file (INI?) for configuring networkb (if needed. We need to
* know wwiv dir and which nodes to send to. and which password to use)
* create way to auto-set password (like wwiv networking did) with binkp

alpha
=====
* start testing single system
* start testing multi system

